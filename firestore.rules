rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // ============================================
    // 🔒 HELPER FUNCTIONS
    // ============================================
    
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isSuperAdmin() {
      return isAuthenticated() && 
        exists(/databases/$(database)/documents/superadmins/$(request.auth.uid));
    }
    
    function isBrandOwner(brandId) {
      return isAuthenticated() && 
        exists(/databases/$(database)/documents/superusers/$(request.auth.uid)) &&
        get(/databases/$(database)/documents/superusers/$(request.auth.uid)).data.brandId == brandId;
    }
    
    // ============================================
    // 🆕 MULTI-BRAND RULES (MVP)
    // ============================================
    
    // Superusers collection
    match /superusers/{userId} {
      // Solo l'utente può leggere i propri dati, o SuperAdmin può leggere tutti
      allow read: if (isAuthenticated() && request.auth.uid == userId) || isSuperAdmin();
      
      // SuperAdmin può creare/aggiornare superusers
      // - create: quando crea nuovo brand con nuovo utente
      // - update: quando aggiunge brand a utente esistente (multi-brand support)
      allow create, update: if isSuperAdmin();
      
      // Nessun delete dal client (solo via backend/console per sicurezza)
      allow delete: if false;
    }
    
    // ============================================
    // 🔐 SUPERADMIN RULES
    // ============================================
    
    // Platform settings
    match /platform_settings/{document} {
      // landing_page è leggibile pubblicamente
      allow read: if document == 'landing_page' || isSuperAdmin();
      
      // Scrittura solo per SuperAdmin
      allow write: if isSuperAdmin();
    }
    
    // SuperAdmins collection
    match /superadmins/{adminId} {
      // Lettura solo per se stessi
      allow read: if isAuthenticated() && request.auth.uid == adminId;
      
      // Nessuna scrittura (gestita manualmente)
      allow write: if false;
    }
    
    // Activity logs
    match /activity_logs/{logId} {
      // Lettura solo per SuperAdmin
      allow read: if isSuperAdmin();
      
      // Scrittura solo per SuperAdmin o system
      allow create: if isSuperAdmin();
      allow update, delete: if false;
    }
    
    // Brands - TEMPORANEO: Lettura/scrittura pubblica per debug
    match /brands/{brandId} {
      // TEMPORANEO: Aperto tutto per debug
      allow read, write: if true;
      
      match /albums/{albumId} {
        allow read, write: if true;
      }
      
      match /settings/{settingsId} {
        allow read, write: if true;
      }
    }
    
    // ============================================
    // 🔙 LEGACY RULES (da migrare)
    // ============================================
    
    // Regole per la collection 'gallery' (sistema vecchio)
    match /gallery/{document=**} {
      // Permetti lettura pubblica
      allow read: if true;
      
      // Permetti scrittura a tutti (temporaneo - da rimuovere dopo migrazione)
      allow write: if true;
    }
  }
}

